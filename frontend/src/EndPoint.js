const endpoint = {
	// make sure you start the backend server first so that it can take port 3000
	allData: 'http://localhost:3000',
	averages: 'http://localhost:3000/averages'
}

export default endpoint
